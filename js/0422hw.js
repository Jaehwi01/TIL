1. let은 일반변수를 선언할때 사용하고/ const는 상수변수를 선언할때 사용한다.const는
2. Object는 객체안의 attribute에 대해서 접근가능/ json 단순 string
3. myObject.key , myObject['key']
4. 1. const header = document.querySelector('h1')
   2. console.log(header.innerText)
   3. header.innerText = 'happy hacking!'


